# Tasks

- [x] Crear `AppLogo` (arte ASCII "TASK MANAGER", `lines()`, `minWidth()`, `fit(width)`)
- [x] `TaskViewRenderer`: dibujar el logo arriba, centrado y en acento; desplazar lista y barra
- [x] Tests: `AppLogoTest` (ancho uniforme, truncado, omisión en terminal angosto)
- [x] Actualizar `README.md` y `project.md`
- [x] Compilar y ejecutar tests (`mvn test`, `mvn package`)
