# Tasks

- [x] Eliminar `MainMenuWindow` y `TaskSelectionWindow` (y sus tests)
- [x] `TaskListWindow`: tecla `a` (crear), quitar `b`, overload con GUI y mensaje inicial
- [x] `LanternaTaskTrackerView` como lanzador de la vista única (`start`)
- [x] `App` abre directamente la lista de tareas y pasa avisos de arranque
- [x] Recortar `TaskTrackerView` y `FakeTaskTrackerView`
- [x] Ajustar `TaskListWindowTest` (quitar `b`, añadir `a`)
- [x] Compilar y ejecutar tests (`mvn test`)
