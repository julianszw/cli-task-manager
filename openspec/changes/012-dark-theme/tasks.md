# Tasks

- [x] Crear `ThemeManager` (estado claro/oscuro, `current()`, selección resaltada)
- [x] `TaskListWindow`: tecla `t`, `ThemeManager`, aplicar tema y repintar
- [x] `TaskCellRenderer`: color de `PENDING` según tema (azul claro / amarillo oscuro)
- [x] Ayuda permanente con la tecla `t` (tema)
- [x] Agregar `ThemeManagerTest` y caso de `t` en `TaskListWindowTest`
- [x] Compilar y ejecutar tests (`mvn test`)
