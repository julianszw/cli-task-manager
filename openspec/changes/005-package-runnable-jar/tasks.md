# Tasks

- [x] Agregar `maven-shade-plugin` (Main-Class + `ServicesResourceTransformer` + filtro de firmas)
- [x] Verificar que `mvn package` genera el fat jar
- [x] Smoke test con `java -jar` (comandos y modo interactivo `list`)
