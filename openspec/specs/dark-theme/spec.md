# Dark Theme (removida)

## Purpose
Esta capacidad fue eliminada. La aplicación ya no alterna entre tema claro y oscuro con la tecla `t`: el lenguaje visual definido en `visual-style` establece un único tema oscuro permanente, sin tecla de alternancia.

## Requirements

_No aplica — capacidad eliminada y reemplazada por `visual-style`._
