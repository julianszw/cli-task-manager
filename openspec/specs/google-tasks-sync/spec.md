# Google Tasks Sync (removida)

## Purpose
Esta capacidad fue eliminada. La aplicación ya no sincroniza de forma bidireccional
entre un archivo JSON local y Google Tasks: ahora opera exclusivamente contra la
Google Tasks API, sin persistencia local. Ver `task-provider`.

## Requirements

_No aplica — capacidad eliminada y reemplazada por `task-provider`._
