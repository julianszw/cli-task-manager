# JSON Persistence (removida)

## Purpose
Esta capacidad fue eliminada. La aplicación ya no persiste las tareas en un archivo
JSON local ni mantiene un repositorio en memoria persistente: la Google Tasks API es
la única fuente de datos. Ver `task-provider`.

## Requirements

_No aplica — capacidad eliminada y reemplazada por `task-provider`._
